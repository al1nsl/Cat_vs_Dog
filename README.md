# 🐶🐱 Классификация кошек и собак для систем умного дома

## 📌 Описание проекта

Данный проект — это **понятное и практическое решение по компьютерному зрению**, которое умеет автоматически определять, **кошка или собака изображена на фото**.

Проект создавался как учебный, но архитектура и подходы соответствуют реальным промышленным решениям и могут применяться в системах **умного дома**, видеонаблюдения и тд.

Реализация охватывает **весь цикл работы с моделью машинного обучения**:
- подготовка и очистка данных
- обучение нейросети
- дообучение (fine-tuning)
- оценка качества
- запуск веб-приложения для пользователей
- веб приложение можно разместить по определенному адресу в сети интернет, тем самым открыть доступ к нему любому человеку

---

## 🎯 Зачем нужен этот проект (простыми словами)

В умном доме зачастую используются камеры. Этот проект показывает, как можно:

- автоматически понять, **какое животное попало в кадр**
- анализировать изображения без участия человека
- реагировать на появление питомца (что позволяет добавить дополнительный функционал, например: отправление уведомления пользователю или воспроизведение определенного сценария)


Пример:
> Камера увидела животное → модель определила, что это собака → система может запереть или открыть дверь → отправить уведомление хозяину.

---

## 🧠 Как работает модель (очень просто)

1. Пользователь или камера передаёт изображение
2. Картинка приводится к нужному размеру
3. Нейросеть анализирует изображение
4. На выходе получается вероятность:
   - ближе к 1 → собака
   - ближе к 0 → кошка
   - (можно сделать и наоборот, нет никакой разницы)

В основе используется **MobileNetV2** — быстрая и эффективная нейросеть, хорошо подходящая для умных устройств.

---

## 📂 Данные

- Используется датасет **PetImages**, в нем примерно 25 000 изображений
- 2 класса: `Cat`, `Dog`

Перед обучением:
- удаляются повреждённые изображения (этот шаг позволяет в дальнейшем избежать ряда проблем)
- данные автоматически делятся на train и test (данные для тренировки и проверки)

---

## 📊 Метрики качества

Качество модели оценивается с помощью:

- **Accuracy** — сколько изображений в целом определено правильно
- **F1-score** — баланс между precision и recall (точностью и полнотой)
- **ROC AUC** — насколько хорошо в общем модель различает классы

Все метрики модели / моделей в дальнейшем можно сохранить в файл `metrics.txt`.

---

# 🚀 Как запустить проект (пошагово)

### 1️⃣🧪 Создание и активация виртуального окружения и его активация (рекомендуется использовать Python 3.13)

## 🪟 Windows (PowerShell)
### Создание виртуального окружения
```bash
python -m venv .venv
````

### Активация окружения:
```bash
.venv\Scripts\Activate
```

### Если активация заблокирована политикой выполнения:
```bash
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```


## 🍎 macOS
### Создание виртуального окружения
```bash
python3 -m venv .venv
```

### Активация окружения:
```bash
source .venv/bin/activate
```

### Активация окружения:
```bash
source .venv/bin/activate
```


## 🐧 Linux
```bash
python3 -m venv .venv
```

### Активация окружения:
```bash
source .venv/bin/activate
```

## После активации окружения в терминале появится префикс:
```
(.venv)
```


### 2️⃣ Установка зависимостей

Создайте виртуальное окружение (рекомендуется) и установите библиотеки:

```bash
pip install -r requirements.txt
```

---

### 3️⃣ Подготовка данных

Скачайте датасет **PetImages** и положите его в папку:

```
data/PetImages/
 ├── Cat/
 └── Dog/
```

### 4️⃣ Очистите повреждённые изображения:

```bash
python scripts/clean_data.py

```

---

### 5️⃣ Обучение модели

Начальное обучение:

```bash
python -m src.train
```

В результате будет создан файл:

```
models/cat_dog_initial.h5
```

---

### 6️⃣ Дообучение модели (fine-tuning)

```bash
python -m src.finetune
```

Результат:

```
models/cat_dog_finetuned.keras
```

Именно этот файл используется для инференса (ликбез: inference — это этап работы модели, когда она уже обучена и делает предсказания на новых данных).

---

### 7️⃣ Оценка качества модели
Для просмотра метрик обученной модели:
```bash
python -m scripts.evaluate_models
```

После выполнения:
- метрики выводятся в консоль, а также сохраняются в отдельный файл `reports/metrics.txt`
### 8️⃣ Запуск веб-приложения

```bash
streamlit run app/app.py
```

Откроется браузер, где можно:
- загрузить изображение
- увидеть результат классификации
- посмотреть уверенность модели

### 9️⃣ Summary: все нужные команды в одном месте
```bash
python scripts/clean_data.py
python -m src.train
python -m src.finetune
python -m scripts.evaluate_models
streamlit run app/app.py
```

---

## 🖥 Как работает каждый скрипт (почти кратко)

- **clean_dataset.py** — удаляет битые изображения (происходит очистка данных)
- **train.py** — обучает модель с нуля (feature extraction)
- **finetune.py** — дообучает последние слои модели
- **evaluate_models.py** — считает метрики качества, выводит их в терминал, а также пишет в файл
- **inference.py** — подготовка изображения для модели (используем при обучении и дообучении, импортируем этот модуль)
- **metrics.py** — пользовательская метрика F1-score (в библиотеке, которую мы используем нет f1-score, в этом модуле мы лично задаем f1-score, можем также его кастамизировать, например: добавить коэффициент β, который помогает выбирать уклон либо в точность, либо в полноту)
- **app.py** — веб-интерфейс на Streamlit (дружелюбный ui для пользователя)
- **config.py** — все основные настройки в одном месте (импортируем этот модуль в других, избегает hardcoding'a, что не является лучшей практикой)

---

## ⚙️ Используемые технологии

- Python 3.13
- TensorFlow / Keras
- MobileNetV2
- NumPy, Pandas
- Streamlit
- PIL (работа с изображениями)
- MLOps-подходы (разделение обучения, инференса, оценки)

---

## 📁 Структура проекта

```
InfaProject/
├── .venv/
│
├── app/
│   └── app.py                  # Streamlit / inference UI
│
├── dataset/
│   └── PetImages/
│       ├── Cat/
│       └── Dog/
│
├── models/                     # сохранённые модели, чекпоинты
│
├── reports/                    # метрики, отчёты, логи экспериментов
│
├── scripts/                    # утилитарные и оффлайн-скрипты
│   ├── clean_data.py
│   ├── fix_images.py
│   └── evaluate_models.py
│
├── src/                        # основной ML-код (package)
│   ├── __init__.py
│   ├── config.py               # конфигурация (paths, hyperparams)
│   ├── data.py                 # dataset / dataloaders
│   ├── metrics.py              # BinaryF1 и кастомные метрики
│   ├── train.py                # базовое обучение
│   ├── finetune.py             # fine-tuning MobileNetV2
│   └── inference.py            # инференс (batch / single image)
│
├── .gitignore
├── requirements.txt
└── README.md

```

---

## ✅ Итог

В результате получена рабочая система, которая:

- умеет распознавать кошек и собак
- очень работает быстро (было оптимизировано множество моментов)
- легко запускается (не нужно обладать навыками программирования, всего лишь нужно выполнять step-by-step)
- может быть использована, а также при появлении необходимости, расширена под системы умного дома или любые другие прикладные задачи

Проект демонстрирует практическое применение **машинного обучения и компьютерного зрения** от обычных данных, расположенных в сети интернет, до готового приложения.
